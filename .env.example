# Copiar a .env y completar. .env nunca se versiona (ver .gitignore).

# Supabase (creado en F-002; no hay proyecto todavía)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=postgresql://user:pass@localhost:5432/swaper

# Docta — feed de cronograma de pagos (cashflow_completo.csv)
DOCTA_TOKEN=
