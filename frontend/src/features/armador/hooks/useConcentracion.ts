/**
 * Se movió a `@/lib/cartera/hooks/useConcentracion` en la Tanda 11 (deuda de `plan.md:2581`).
 * Re-exportado acá para no editar los importadores existentes del armador.
 */

export { useConcentracion } from '@/lib/cartera/hooks/useConcentracion'
export type { PosicionConPeso } from '@/lib/cartera/hooks/useConcentracion'
