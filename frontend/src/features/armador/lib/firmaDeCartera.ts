/**
 * Se movió a `@/lib/cartera/firmaDeCartera` en la Tanda 11 (deuda de `plan.md:2581`). Re-exportado
 * acá para no editar los importadores existentes del armador.
 */

export { firmaDeCartera } from '@/lib/cartera/firmaDeCartera'
