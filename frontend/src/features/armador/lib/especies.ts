/**
 * Se movió a `@/lib/cartera/traerUniversoEntero` en la Tanda 11 (deuda de `plan.md:2581`, saldada
 * para que el diagnóstico de cartera de F-030 traiga el universo con la misma función). Re-
 * exportado acá para no editar los importadores existentes del armador.
 */

export { traerUniversoEntero } from '@/lib/cartera/traerUniversoEntero'
