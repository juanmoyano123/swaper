/**
 * Se movió a `@/lib/cartera/bloques` en la Tanda 18 (F-042). Re-exportado acá para no editar los
 * importadores existentes del armador.
 */

export {
  agruparEnBloques,
  BLOQUE_POR_CLASE_ACTIVO,
  ORDEN_DE_BLOQUES,
  ROTULO_DE_BLOQUE,
  type BloqueDeCartera,
  type IdBloque,
} from '@/lib/cartera/bloques'
