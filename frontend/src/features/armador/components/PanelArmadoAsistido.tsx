/**
 * Stub de base común — F-019, Tanda 10.
 *
 * Archivo propio para que F-019 no tenga que tocar `ArmadorPage.tsx` (que queda congelado durante
 * la ejecución en paralelo) ni compartir archivo con F-022. Mismo criterio que
 * `PanelesDeLaCartera.tsx` en la Tanda 9: un archivo por feature, aunque el contenido de arranque
 * sean tres líneas.
 */
export function PanelArmadoAsistido() {
  return null
}
