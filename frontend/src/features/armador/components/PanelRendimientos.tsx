/**
 * Stub de base común — F-022, Tanda 10.
 *
 * Archivo propio para que F-022 no tenga que tocar `ArmadorPage.tsx` (que queda congelado durante
 * la ejecución en paralelo) ni compartir archivo con F-019. Mismo criterio que
 * `PanelesDeLaCartera.tsx` en la Tanda 9: un archivo por feature, aunque el contenido de arranque
 * sean tres líneas.
 */
export function PanelRendimientos() {
  return null
}
