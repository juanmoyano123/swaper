/**
 * Vector de riesgo de seis ejes de la cartera en construcción — F-031 (tanda 12).
 *
 * Stub de la base común: cuerpo pendiente. Se monta en `ArmadorPage.tsx` después de
 * `PanelConcentracion`, siguiendo el mismo criterio de congelamiento de las tandas 9-11 — este
 * archivo lo reemplaza F-031 entero, nadie más lo toca.
 */
export function PanelRiesgo() {
  return null
}
