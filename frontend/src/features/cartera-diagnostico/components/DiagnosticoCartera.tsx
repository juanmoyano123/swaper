/**
 * Stub de base común de la Tanda 11 — F-030 (valuación y diagnóstico de la cartera cargada).
 *
 * Reemplazado por la implementación completa durante la ejecución de F-030. Ver
 * `claude-docs/plans/F-030-plan.md`.
 */

import type { PosicionCruda } from '@/features/cartera-ingreso/types'

export function DiagnosticoCartera({ posiciones }: { posiciones: PosicionCruda[] }) {
  void posiciones
  return null
}
